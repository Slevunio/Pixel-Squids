export interface ITrack {
    id: string;
    name: string;
    soundTrack: string; //will be Blob
    instrumentType: string;
    notes: string; //will be Blob
    createdAt: string;
    updatedAt: string;
}